<div class="container">
  <h5 class="col-md-7 mb-5 mt-5">
    <span class="px-3 ml-5 bg-ligh alert-secondary">ジャンル編集</span>
  </h5>

    <div class="row mt-5">
        <%= form_with model: [:admin, @genre] do |f| %>
          <span class="mr-5">ジャンル名</span>
          <%= f.text_field :name %>
          <%= f.submit "変更を保存", class: "ml-5 btn btn-success" %>
        <% end %>
    </div>
</div>