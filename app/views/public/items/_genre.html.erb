<div class="border border-dark">
  <p class="border-bottom border-dark text-center"><strong>ジャンル検索</strong></p>
    <div>
      <!-- ジャンル呼び出し-->
      <ul class="list-unstyled">
        <% genres.each do |genre| %>
          <li class="my-2 ml-3"><%= genre.name %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>